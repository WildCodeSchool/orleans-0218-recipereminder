{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container>">
        <h1 class="text-center"> {{ title }}</h1>
        <div id="list">
            <div class="row">
            {% for thumb in list %}
                {% include 'thumb.html.twig' %}
                {% else %}
                <p> Aucun élément à afficher.</p>
            {% endfor %}
            </div>
        </div>
    </div>

{% endblock %}
